# PR41 审查修复 — 进展报告

- 分支：`fix/pr4-transaction-phase2`（基于 `integration/data-integrity-transaction` f700713b）
- 修复提交：`b3c9105` fix(execution): close recovery protocol gaps flagged in PR41 review
- 规模：11 files changed，+1068 / -248
- 状态：**7 项审查意见全部修复完毕，测试全绿，待 push**

---

## 一、审查意见处理总览

| # | 审查项 | 级别 | 状态 |
|---|--------|------|------|
| 1 | recovery 误杀仍在执行的并发 Worker 事务 | P0/P1 | ✅ 已修 |
| 2 | Orchestrator 状态 UNKNOWN 被当成"应回滚" | P1 | ✅ 已修 |
| 3 | 发现 unresolved recovery 仍继续执行新事务 | P0 | ✅ 已修 |
| 4 | Critic State 的 report_path 指向 task-local 路径 | P1 | ✅ 已修 |
| 5 | transactional evidence 绕过完整 EvidenceEvent 契约 | P1/P2 | ✅ 已修 |
| 6 | artifact copy 后未复验 sha256（TOCTOU） | P2 | ✅ 已修 |
| 7 | execute_task 过长 + complete() 成功后失败语义混乱 | P1/P2 | ✅ 已修 |

并发模型决策（主人拍板）：**lease/heartbeat + startup-only 兜底两者都要**。

---

## 二、逐项修复说明

### 1. Recovery 不再误杀活事务（Blocker 1）

根因：`execute_task()` 每次启动全局扫描 `.staging/*/metadata/commit.json`，只要 DB COMMITTED 且 Orchestrator 未显示 succeeded 就回滚。而正常链路 `DB commit → refresh_projections → complete()` 之间天然有窗口，并发 Worker 会在窗口内误杀对方事务。

修复（`execution/recovery.py`、`execution/commit_manager.py`）：

- commit marker 写入 `owner_worker_id` 与 `heartbeat_monotonic`。
- 新增 staleness 门控（默认 300s）：心跳仍新鲜的事务视为"owner 可能还活着"，抛 `_SkippedActive` 跳过，记入 `RecoveryResult.skipped_active`，绝不回滚。
- 无心跳信号的历史 marker（崩溃遗留）按原逻辑正常恢复，保证旧崩溃事务仍可被收敛。
- `CommitManager.refresh_heartbeat()` 提供 commit 窗口内的心跳刷新。

### 2. Orchestrator 状态三态化（Blocker 2）

根因：`_orchestrator_closed_for_transaction()` 把所有异常一律 `return False`，recovery 把 False 解释成"未关闭 → 补偿"。一次临时读文件失败就会触发破坏性回滚。

修复（`execution/recovery.py`）：

- 新增 `probe_orchestrator_state()` 返回三态：`closed` / `open` / `unknown`。
- `unknown`（快照读失败、JSON 损坏、缺 run path）→ marker 标 `RECOVERY_UNRESOLVED`，记入 unresolved，**绝不补偿**。
- 原则：数据完整性系统里，"不知道"不等于"没关闭"。

### 3. Unresolved recovery 必须 fail-closed（Blocker 3）

根因：`RecoveryManager` 已记录 `unresolved_transactions` / `marker_errors`，但 `execute_task()` 完全不检查，继续在半恢复状态上产生新的正式数据。

修复（`execution/worker.py`）：

- `recover_pending()` 返回结构化 `RecoveryResult`（recovered / unresolved / marker_errors / skipped_active，`clean` 属性）。
- 新增 `RecoveryError` 与 `_assert_recovery_clean()`：recovery 结果不干净即抛错，拒绝启动新事务，错误信息携带具体 transaction id 与 marker 路径。

### 4. Critic State 绑定 committed artifact（Blocker 4）

根因：handler 在 staging 之前把 `task_dir/outputs/critic_report.json` 写进 `state_updates["critic"]["report_path"]`；commit 后只有 `result.outputs` 被替换成正式路径，State 仍指向可变、可能被清理的 task 工作副本。

修复（`contracts/critic.py`、`execution/handlers.py`、`execution/adapters.py`、`agents/planner/service.py`）：

- `HandlerContext` 新增 `transaction_id` 与 `artifact_id_for(role)`，handler 在 commit 前即可预知正式 artifact 身份（命名规则与 adapter staging 保持一致）。
- `critic_persistence_effects()` 支持 `report_artifact_id`；事务路径下 State 只存 `report_artifact_id + report_sha256`，不再存 task-local path。
- 新增 `resolve_critic_report_path(critic, store)`：优先从 artifact registry resolve，回退 legacy `report_path`（旧数据兼容）。
- Planner `_plan_progress_step` 改用 resolver。

### 5. Transactional evidence 统一契约（附带 5）

根因：`EvidenceLogger` 走完整 `EvidenceEvent` 校验，事务 commit 的 evidence 直接 `_append_event()` 只验 3 个字段，形成两套写入标准。

修复（`execution/worker.py`、`contracts/event.py`）：

- `_formalize_evidence_events()`：commit 前把每个事务 evidence 构造成正式 `EvidenceEvent`（补齐 timestamp / event_id / trace 字段），任何契约违规直接阻断 commit。
- `VALID_EVENT_TYPES` 补登 `execution_transaction_committed` / `execution_transaction_failed`——这两个 store 内部写入账本的事件类型此前不在契约枚举里，属于"两套标准"的残留。

### 6. Artifact copy 后复验 sha256（附带 6）

修复（`execution/commit_manager.py`）：`copyfile + fsync` 之后对 temporary 重新 `file_sha256`，与 stage 时登记的摘要不一致则拒绝提交。堵住 validate → copy 之间内容被换导致 registry 记旧 hash、文件是新内容的 TOCTOU 窗口。

### 7. complete() 不可逆成功边界 + execute_task 拆分（结构 7）

根因：`complete()` 成功后 receipt/evidence/cleanup 任一失败仍走统一失败分支重抛——Orchestrator 已成功、调用方却收到失败，触发重试造成"用户看到失败、系统已成功"的双重事实。

修复（`execution/worker.py`）：

- `complete()` 之后所有后处理收进 `_post_commit_finalize()`：receipt 写盘、完成 evidence、staging 清理各自独立 try，失败只记入 `receipt["post_commit_warnings"]`，绝不重抛。
- `execute_task` 按职责拆分，"什么时候还能回滚"写进结构：

| 函数 | 行数 | 职责 |
|------|------|------|
| `execute_task` | 53 | claim + 阶段编排 + 异常分发 |
| `_run_claimed_task` | 110 | packet 校验 → recovery 门控 → 执行并提交事务 |
| `_close_orchestrator` | 51 | complete() 成功边界 + receipt 构建 |
| `_post_commit_finalize` | 46 | 成功边界之后的 bookkeeping（永不重抛） |
| `_finalize_failure` | 100 | 失败补偿 / Orchestrator fail / 失败 evidence |

---

## 三、测试

### 新增强制测试（7 个，全部对准审查点）

| 测试 | 文件 | 验证点 |
|------|------|--------|
| `test_recovery_skips_transaction_with_live_owner_heartbeat` | test_data_integrity_transactions.py | Blocker 1：活心跳事务不被回滚 |
| `test_recovery_unknown_orchestrator_state_never_compensates` | 同上 | Blocker 2：UNKNOWN 标 unresolved 不补偿 |
| `test_execute_task_refuses_to_start_on_unresolved_recovery` | 同上 | Blocker 3：fail-closed 门 |
| `test_critic_state_binds_committed_artifact_not_task_path` | test_transactional_handlers.py | Blocker 4：State 无 task 路径、registry resolve、sha 一致 |
| `test_transactional_evidence_satisfies_full_event_contract` | 同上 | 附带 5：每条 committed evidence 过完整契约 |
| `test_transactional_evidence_contract_violation_blocks_commit` | 同上 | 附带 5：契约违规阻断 commit 且无残留 |
| `test_artifact_content_changed_during_copy_is_rejected` | test_data_integrity_transactions.py | 附带 6：copy 窗口篡改拒绝 |
| `test_post_commit_bookkeeping_failure_is_not_a_task_failure` | test_execution.py | 结构 7：complete 成功后后处理失败不重试 |
| `test_orchestrator_probe_unknown_on_unreadable_snapshot` | test_execution.py | Blocker 2：probe 异常路径 |

### 回归结果

- 核心套件（contract_migration / critic / data_integrity_transactions / data_layer / execution / orchestrator / planner / storage / threshold_calibration / transactional_handlers / project_context）：**132 项全过**。
- 其余（prediction_pipeline / target_bootstrap / threshold_research / web_api / reliability_regressions）：**92 项全过**。
- reliability suite 此前的 import failure 是缺 `biotite` 的环境问题，已在隔离 venv 补装，套件恢复可跑。
- 既有 recovery 测试同步迁移到新 API（三态 probe + RecoveryResult）。

---

## 四、边界确认

- 未触碰：科学算法、threshold 数值、Planner Action 语义、Prediction 迁移（PR40 未就位，继续冻结）。
- 未引入新基础设施；无第二套 transaction abstraction。
- legacy `report_path` 数据行通过 resolver 回退保持可读；旧无心跳 marker 仍可被正常恢复。
- `git diff --check` 干净，无无关改动。

## 五、待办

- [ ] push `b3c9105` 到 `origin/fix/pr4-transaction-phase2`（等本机 gh 凭据）
- [ ] push 后 PR41 由 draft 转 ready，重新走 review
