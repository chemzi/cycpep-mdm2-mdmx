# PR41 审查修复计划

- 审查对象：PR #41，`integration/data-integrity-transaction (f700713b)` ← `fix/pr4-transaction-phase2 (b86f7a82)`
- 规模：5 commits / 22 files / +1597 -160
- 结论：**Request changes，不合入**。7 条审查意见经本地代码逐条核实，全部属实。
- 工作分支：继续在 `fix/pr4-transaction-phase2` 上修改；未经授权不合并 integration。
- 前提：远端当前无 PR40 专用分支，Prediction 事务迁移保持冻结，不在本轮范围。

---

## 一、Merge Blocker（必须修复，缺一不合）

### Blocker 1 — Recovery 会回滚仍在正常执行的并发 Worker 事务（P0/P1）

**现状（已核实）**
- `execute_task()`（execution/worker.py）每次启动都对 `execution_root/.staging/*/metadata/commit.json` 做全局 `recover_pending()`。
- `recovery.py` 的 `_recover_commit_window()`：只要 DB 已 COMMITTED 且 `orchestrator_closed()` 返回 False，就直接 `_finish_compensation()` → `rollback_transaction()` → 删 artifact。
- 正常链路 `DB commit → refresh_projections → complete()` 之间天然存在窗口，另一个 Worker 恰好在此窗口启动就会把对方活事务回滚，造成 Orchestrator 记成功但数据已被撤销。

**修改**
- marker 写入 owner 信息（worker_id）与心跳/mtime；recovery 只对“确认 owner 已死”的事务动手。
- 引入 staleness 判定：mtime 未超过阈值的 PREPARED/COMMITTED marker 视为“可能仍活跃”，跳过不补偿。
- `recover_pending()` 返回结构化 `RecoveryResult`（recovered / unresolved / marker_errors / skipped_active），不再只回 list。

**测试**
- 双 Worker barrier 并发用例：Worker A 处于 DB COMMITTED 未 complete 窗口，Worker B 跑 recovery，A 的事务必须不被回滚。

### Blocker 2 — Orchestrator 状态 UNKNOWN 被当成“应回滚”（P1）

**现状（已核实）**
- `_orchestrator_closed_for_transaction()` 把 `OSError / JSONDecodeError / KeyError / OrchestratorContractError` 一律 `return False`。
- recovery 把 False 解释成“未关闭 → 补偿”。一次临时读文件失败就会触发破坏性回滚。

**修改**
- 改为三态：`CLOSED`（保留）/ `OPEN_OR_FAILED_CONFIRMED_DEAD`（可补偿）/ `UNKNOWN`（禁止补偿，记入 unresolved）。
- UNKNOWN 时 marker 标注 `RECOVERY_UNRESOLVED`，绝不补偿。

**测试**
- 模拟 run snapshot 读取失败/JSON 损坏，断言对应事务不被回滚且进入 unresolved。

### Blocker 3 — 发现 unresolved recovery 仍继续执行新事务（P0）

**现状（已核实）**
- `RecoveryManager` 维护了 `unresolved_transactions` / `marker_errors`，但 `execute_task()` 调完 `recover_pending()` 后完全不检查，继续在半恢复状态上产生新 Candidate/State/Evidence。

**修改**
- `execute_task()` 在 recovery 后做 fail-closed 检查：`unresolved` 或 `marker_errors` 非空则抛出明确的 recovery 错误，拒绝启动新事务，并把 transaction id / marker 路径暴露给诊断。

**测试**
- 预置一个 compensation conflict / 损坏 marker，断言新任务被拒绝执行且报错含具体 transaction id。

### Blocker 4 — Critic State 的 report_path 指向非正式 task 路径（P1）

**现状（已核实）**
- `execution/handlers.py:458-460` 在 staging 之前就把 `task_dir/outputs/critic_report.json` 写进 `state_updates["critic"]["report_path"]`。
- commit 后 worker 只替换了 `result.outputs`，未回写 state。结果 artifact registry 是正式路径，`State.critic.report_path` 仍指向可变、未来可能被清理的 task 工作副本。

**修改**
- State 存 `report_artifact_id + report_sha256`，path 从 artifact registry resolve，不持有 task-local 路径。
- commit 完成、拿到 committed artifact 路径后回写 state（或由 registry 解析），保证 state 与 registry 一致。

**测试**
- 断言 `state["critic"]["report_path"] == store.get_artifact(artifact_id)["path"]`，且 `file_sha256(state 的 path) == state["critic"]["report_sha256"]`。

---

## 二、强烈建议随本 PR 一并修（不阻断，但建议同轮解决）

### 5 — Transactional Evidence 绕过完整 EvidenceEvent 契约（P1/P2）
- 现状：`EvidenceLogger._write()` 走 `EvidenceEvent.from_dict()` 完整校验；事务 commit 的 evidence 直接 `_append_event()`，worker 只验 agent/event_type/phase 三字段，形成两套写入标准。
- 修改：commit 前把 transactional evidence 统一构造成正式 `EvidenceEvent`（补齐 timestamp/event_id/trace/failure payload 等约束）。
- 测试：构造缺 trace 字段/坏 failure payload 的 evidence，断言 commit 阶段被拒绝。

### 6 — Artifact copy 后未复验 SHA（TOCTOU，P2 hardening）
- 现状：`_prepare_artifacts()` 在 `copyfile → fsync` 后直接登记 stage 时的旧 sha；validate→copy 之间被改会导致 registry 记旧 hash、文件是新内容。
- 修改：`shutil.copyfile + fsync` 后对 temporary 重新 `file_sha256`，与 `artifact.sha256` 不一致则拒绝。
- 测试：在 copy 窗口内篡改 staged 文件，断言 commit 拒绝。

---

## 三、结构性问题（按 ENGINEERING_STANDARD 顺手收敛，不扩大范围）

### 7 — execute_task 过长 + complete() 成功后的失败语义混乱（P1/P2）
- 现状：`execute_task()` 约 200 行，claim/校验/transaction/recovery/handler/commit/orchestrator closure/receipt/evidence/补偿全在一个 try 里；`complete()` 成功（`orchestrator_closed=True`）后，receipt/evidence/cleanup 任一抛异常仍走统一失败分支重抛——Orchestrator 已成功、调用方却收到失败，触发重试会造成“用户看到失败、系统已成功”的双重事实。
- 修改：
  - 把 `complete()` 作为不可逆成功边界：其后 receipt/evidence/cleanup 失败降级为 `post_commit_warning`，记录但不重抛、不触发业务重试。
  - 按职责拆出“准备与恢复 / 执行并提交事务 / 关闭 Orchestrator / 成功后处理 / 失败与补偿”几个函数，让“什么时候还能回滚、什么时候不能”写进结构。
- 测试：
  - `complete()` 成功后人为让 receipt 写失败，断言对调用方不表现为可重试的普通失败。
  - 存在 unresolved recovery 时禁止启动新事务（与 Blocker 3 共用）。

---

## 四、执行顺序与回归

1. Blocker 1 → 2 → 3 → 4（recovery 协议是一组，一起改一起测）。
2. 附带项 5、6。
3. 结构项 7 最后做，避免和 recovery 改动互相干扰。
4. 每组改动后跑直接相关测试，最后跑尽可能完整的回归（contract / transaction / reliability suite）。
5. merge gate：diff 无无关改动、public contract 兼容、无新增长函数/重复 abstraction、异常不吞原始错误、`git diff --check`、严格 review。
6. 通过后 `commit → GitHub Connector push fix/pr4-transaction-phase2`，不自动 merge integration。

## 五、明确不做

- 不启动 Prediction transactional migration（PR40 未就位，继续冻结）。
- 不引入 Redis/Kafka/ORM/2PC/EventStore 等新基础设施。
- 不为 artifact 完整性以外的目的新增 hash。
- 不为理论安全性处理现实中基本不可能出现的 case。
