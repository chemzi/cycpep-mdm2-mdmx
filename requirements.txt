# Core Python dependencies used by the current checked-in code.
# Install with:
#   pip install -r requirements.txt
#
# Notes:
# - This file lists Python packages only.
# - Several planned tools in the v5 design document are external frameworks
#   or server-side programs and are therefore documented below as comments
#   rather than pinned pip requirements.

# Keep the NumPy 1.x ABI until the full scientific stack is rebuilt for 2.x.
numpy>=1.24,<2
biotite>=0.41

# Model runtimes are deployed separately from this small development/test set:
# torch                  # RFdiffusion / ProteinMPNN runtime
# colabdesign            # AfCycDesign and Prediction workers
# ProteinMPNN            # invoked through a pinned LigandMPNN repository checkout
# boltz[cuda]==2.2.1     # Prediction independent model family; isolated GPU env

# Planned external tools from the project plan (not normal pip deps here):
# - RFpeptides / RFdiffusion
# - LigandMPNN
# - AfCycDesign / ColabFold
# - HADDOCK
# - Rosetta FastRelax / InterfaceAnalyzer (license-gated external distribution)
# - PRODIGY
# - RDKit
#
# Why RDKit is not included above:
# - It appears in the v5 plan for Critic / synthesizability checks,
#   but the current checked-in Python code does not import it yet.
