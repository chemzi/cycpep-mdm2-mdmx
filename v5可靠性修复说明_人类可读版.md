# v5 可靠性修复说明：Research、Design 与共享数据层

## 一、这次修改解决什么问题

本地修复分支为 `codex/v5-research-data-reliability`，起点是远端 `main` 的提交 `8400721272682e7d7fbacefb75baf4bdac9d099f`。本次工作围绕一条完整的数据责任链展开：Research 产生的靶标、结构和文献证据需要能够被核查；Design 只有在生成真实有效输出后才能登记候选；Prediction 读取候选时需要知道环化化学、设计结构和每个指标的计算方法；组长看到 `all_layers_pass=True` 时，该字段需要严格表达 MDM2 与 MDMX 两侧均完成七层检查。

所有联网验证都在 `/tmp` 下的隔离副本中执行。隔离副本没有复制 `_research_cache.json`、`state.json`、`candidate_index.csv`、`evidence_log.jsonl` 或已经下载的 `targets/`，因此本说明中的 Research 数量来自新代码从 RCSB 和 PubMed 重新获取的结果。仓库内原有运行数据没有被测试覆盖。线上 PR 尚未创建，分支也没有推送。

## 二、Research Agent 原有问题、形成原因与修复方式

### 2.1 PDB 搜索标签曾经混淆“搜索词”和“结构中的真实蛋白”

旧版 `scripts/search_pdb.py` 的 `_search_target(target_name, uniprot)` 接收 UniProt 参数，但 RCSB 查询仅使用 MDM2 或 MDMX 的全文关键词。一个结构标题可能同时出现两个名称，MDM2 与 MDMX 的检索结果因此大量重叠。紧接着的 `scripts/enrich_pdb.py` 虽然从 GraphQL 取得实体 UniProt，却继续把最初的搜索批次直接写入 `target`。这样得到的 `target="MDM2"` 只能说明该 PDB 被 MDM2 关键词搜到，无法证明界面中的蛋白链属于 MDM2。

新版搜索条件直接使用 RCSB polymer entity 的 UniProt accession：MDM2 为 `Q00987`，MDMX 为 `O15151`。GraphQL 富集再次逐实体核对 UniProt，并保留 author chain ID、asym chain ID、实体序列和实验方法。一个 PDB 同时含有两个靶标时，系统会生成两条 target-specific 记录，每条记录只携带对应靶标链。搜索结果重叠现在表达真实的多蛋白组成，不会造成标签串线。

### 2.2 界面计算曾把其他大分子也并入靶标

旧版 `scripts/compute_interface.py` 用 6–50 aa 的长度规则找肽链，随后把所有剩余原子视为靶标。晶体结构若含有抗体、融合标签、第二种蛋白或其他肽链，这些原子会参与距离计算并产生错误界面残基。脚本还直接截取混合列表前 10 条；旧标签主要是 MDM2，实跑结果因而出现 MDM2=10、MDMX=0。

新版从 GraphQL 结果读取明确的 peptide chain ID 和 UniProt-classified target chain ID，只计算这两组链之间小于 4 Å 的重原子接触。输入没有靶标链注释时会返回可读错误，长度规则只保留为单独调用底层函数时的肽链 fallback。采样按照 MDM2 与 MDMX 分组，每侧默认最多 10 条，可通过 `MAX_PDBS_PER_TARGET` 调整。完整实跑得到 20 条有效界面，其中 MDM2=10、MDMX=10。

### 2.3 跨结构口袋聚合曾受链名和字符串顺序影响

旧版把 `A:58GLY`、`X:58GLY` 当作两个不同残基，因此同一个 UniProt 位点在不同 PDB 使用不同链名时无法形成共识。口袋匹配又使用 `Gly58 in A:58GLY` 这种字符串包含关系，残基名与编号顺序相反，实际无法命中。

新版聚合时先去掉结构特有的链名，把残基标准化为 `58GLY`，同一结构中的多个拷贝只计一次；随后把参考定义 `Gly58` 解析成同样的编号与三字母残基名进行精确比较。动态口袋只有在 MDM2 与 MDMX 两侧均得到有效结构时才会启用，单侧成功会保留为运行信息并使用经过标记的 curated fallback，避免把单靶结果描述成双靶动态结论。

### 2.4 1YCR 与 3DAB 的叠合需要真实结构运行后才能发现编号偏移

旧版按照相同 residue ID 配对 Cα。隔离实跑最初得到 85 对 Cα、RMSD 3.964 Å；查看真实序列后可见两条链存在残基编号与插缺偏移，同号残基并不总是同源位置。这个问题在空目录或旧缓存上无法判断。

修复后先对两个结合域的氨基酸序列做 Needleman–Wunsch 全局比对，再按比对产生的同源位置配对 Cα。利用刚刚下载的 1YCR 和 3DAB 重跑得到 85 对 Cα、序列一致性 0.553、Cα RMSD 1.500 Å。SASA 也按结构所属靶标的口袋编号分别计算，避免在 MDM2 结构上同时套用 MDMX 残基定义。此次结果为 MDM2 三个口袋 SASA 378.0、194.3、420.0 Å²，MDMX 为 403.4、225.5、301.3 Å²；这些数值应作为当前参考结构与当前算法的计算结果记录，不应外推成结合自由能或普遍规律。

### 2.5 PubMed 摘要与阈值证据需要精确对应 PMID

旧版 `pubmed_search.py` 从 ESummary 读取 `abstract` 字段，ESummary 通常不提供摘要，非 PMC 论文内容因此为空。新版使用 EFetch XML 建立 PMID 到摘要的明确映射；有 PMC 全文时仍优先使用全文，没有全文时保存真实 PubMed 摘要，二者均缺失时标为 `metadata_only`。

阈值检索原先在提示词中写入 pLDDT、ipSAE、dG、SC、dSASA、N–C 距离和 RMSD 等候选数字，模型很容易复述这些数字并把它们包装成文献结论。旧代码还会把 `field_consensus` 和 `estimate` 自动覆盖到运行阈值中，同时丢弃 evidence grade。新版移除了数值提示，要求模型返回 `source_pmid`、包含指标名和数值的摘要原句、`paper_explicit` 证据等级和论文中的具体用途。代码会确认 PMID 属于本轮检索集合，并将规范化后的证据原句逐字匹配到该 PMID 摘要。只有四项校验全部通过时才允许自动覆盖。其他输出保留为检索失败或待标定信息。

本地没有配置 LLM API key。本次完整运行仍完成 30 篇 PubMed 检索，LLM binder 提取状态为 `degraded_no_api_key`，阈值检索状态为 `degraded_no_llm`，自动文献阈值覆盖数为 0。State 中的 8 条 binder 来自有明确标记的 curated fallback；9 项阈值保留暂定值、证据等级和 `calibration_status="pending"`。运行日志能够区分工具成功、可选模型不可用和 fallback 来源。

## 三、共享数据层为什么需要迁移

仓库当前 `data/candidate_index.csv` 使用 25 列旧表头，新版 `INDEX_COLUMNS` 已经扩展。旧实现只在文件不存在时创建新表头，现有 CSV 会继续保留 25 列；调用 `CandidateIndex.add()` 后，新行可以携带更多值写入旧表，下一次 `update_score()` 才会因额外字段或无表头值报错。这个故障在全新临时表上不会出现，必须用旧表头复现。

现在 `CandidateIndex._ensure_exists()` 会比较实际表头与当前 schema。发现旧表时，程序先在同目录生成 `candidate_index.pre_v5_<UTC>.csv` 备份，再把每行迁移到新表，并通过同目录临时文件加 `os.replace()` 原子替换。迁移事件写入 EvidenceLogger，记录旧列名、新列数、行数和备份路径。测试使用独立临时数据目录，原有 `data/state.json`、候选表和证据日志不再被测试脚本删除。

旧字段中只有 `monomer_plddt` 与当前 `plddt` 含义一致，可以安全迁移。HADDOCK score 与 PRODIGY 预测 dG 使用不同模型和数值尺度；旧 `self_rmsd` 缺少当前 scRMSD 的结构定义；旧 `layer2_3_pass` 混合多个层；旧 `layer4_pass` 与新版环闭合层含义不同。因此这些字段分别进入 `legacy_haddock_*`、`legacy_self_rmsd`、`legacy_layer2_3_pass`、`legacy_layer4_pass` 等列，只用于历史追溯，不参与 v5 七层判定。

新表增加了 `cyclization_type`、`cyclization_bonds`、`design_pdb_path`、`design_pdb_hash` 和 `manifest_path`。L4 保存 relax 前后的 `nc_distance_pre` 与 `nc_distance_post`；L5 保存两个靶标各自的热点覆盖和位点一致性；L6 保存两个靶标各自的 pose RMSD 与 seed convergence。完整文件哈希改为流式 SHA-256 的前 12 位，避免仅采样 PDB 前 4096 字节导致不同模型共享同一哈希。

`evaluate_battery()` 默认 `required_targets=("MDM2", "MDMX")`，L2、L3、L5、L6 都需要两个靶标分别满足条件。单靶正对照标定可调用 `required_targets=("MDM2",)` 或 `("MDMX",)`。L4 使用可审计的 N–C 数值，显示用的布尔字段不再单独决定通过。新增 `compute_pareto_front()` 根据 MDM2 与 MDMX 两个目标返回非支配候选 ID，避免把任意加权总分写成最终门槛。

## 四、Design Agent 的修复边界

Route A 的 `/root/targets`、`/root/designs` 和 ColabDesign 参数目录已经改为环境变量控制，默认输出位于仓库数据目录。靶标 PDB、输出 PDB、生成脚本和运行参数进入 manifest。子进程退出码为 0、输出 PDB 存在、提取序列长度正确且序列合法时才登记候选；失败尝试只写 error evidence。ColabDesign cyclic offset 表达循环位置编码，不能证明输出 PDB 已有真实首尾共价键，因此 manifest 明确写入 `closure_representation="cyclic_residue_offset_not_covalent_bond"`，Prediction 仍需构建化学闭环、relax 并测量前后 N–C 几何。

Route B 原实现调用 `proteinmpnn.run.get_model, score_seq`，这个接口与常见官方 ProteinMPNN 代码布局不一致。异常发生后旧代码返回原模板，却继续以“ProteinMPNN 优化候选”登记。新版无法获得有效适配器输出时返回空结果并记录错误，原模板不会冒充模型输出。模板优先从 Research 写入 State 的真实 binder 序列读取；Research 尚未运行或没有合法序列时才使用带有 `curated_fallback` 标记的常量。Route B 的正式恢复需要于嘉乐基于实际安装版本实现并测试 ProteinMPNN/LigandMPNN adapter。

Route C 原先把 C–C、C–S、S–C 和空端基混入同一“二硫键/酰胺键”矩阵，其中只有 C–C 能形成标准二硫键。扩展循环还用裁剪后序列长度作为位置偏移，计算出的突变索引经常落在序列外，`n=200` 时可能长期无法增加候选。新版按当前 v5 产品定义统一生成首尾酰胺键意图，使用 0/2/3/4 aa Gly/Ser linker，并保留 F/W/L 锚点对其余位置做确定性、去重的单点枚举。隔离测试实际生成 200 条候选，返回数、索引数和唯一序列数均为 200，每条都有 manifest 和明确成键原子。

## 五、完整实跑结果与状态解释

| 阶段 | 新代码隔离实跑结果 | 如何解释 |
|---|---:|---|
| RCSB UniProt search | MDM2 50，MDMX 19 | 两个查询均成功；数量受每个查询最多 50 条限制 |
| GraphQL enrich | 43 条 target-specific peptide-complex 记录 | 已按实体 UniProt 与链 ID 分类 |
| Interface calculation | 20 条有效界面 | 每个靶标默认各取 10 条 |
| Pocket aggregation | MDM2 10，MDMX 10 | 双侧均有数据，允许启用 dynamic pockets |
| Sequence-aware superposition | 85 对 Cα，RMSD 1.500 Å | 修复同号残基错配后的结果 |
| PubMed | 30 篇 | EFetch 摘要或 PMC 正文进入下游 |
| LLM binder extraction | degraded_no_api_key | 使用 8 条 curated fallback，来源有标记 |
| Threshold extraction | degraded_no_llm，0/9 自动覆盖 | 暂定阈值等待正对照标定 |
| Overall Research run | degraded_with_fallbacks | 结构与 PubMed 成功，可选 LLM 步骤缺少凭据 |

这里的 `degraded_with_fallbacks` 仍然允许 Design 使用经过核查的结构热点和 curated binder，但展示和报告必须保留来源标签。它不等价于整个 Research 失败，也不等价于八步全部完成。配置 LLM 后需要重跑阈值证据校验；模型输出即使成功，也只有通过 PMID 与摘要原句验证的 `paper_explicit` 数值能进入阈值表。

## 六、一个月赛期内仍需完成的工作

这次 PR 处理数据可靠性与模块交接，没有把当前 Design 宣称为 v5 文档中的完整 RFpeptides 流程。Route A 仍以 ColabDesign fixbb cyclic offset 为原型；正式路线需要在服务器安装并锁定 RFdiffusion/RFpeptides、LigandMPNN 或 ProteinMPNN、AfCycDesign/ColabFold 与 Rosetta 的版本和模型权重，然后用正对照先跑通。Prediction、Planner 和 Critic 文件目前仍是 TODO 骨架，七层字段已经具备，真实打分工具和长时回溯策略仍需实现。

一个月内最优先的实验是纯 in silico 正对照校准。分别选取 MDM2 和 MDMX 已知肽复合物，经过与候选完全相同的结构准备、预测、PRODIGY、Rosetta、环闭合与多 seed 流程，得到每个工具在本项目环境中的分布。阈值可根据正对照召回、负对照区分和算力预算确定，并把计算方法、版本、输入哈希和校准日期写回 State。由于竞赛不安排 wet lab，最终结论需要使用“计算候选”“预测结合模式”“通过当前 in silico battery”等措辞，避免把预测分数写成实测亲和力或临床可开发性。

## 七、三位同学的交接方式

张义忱负责共享数据与集成时，需要重点审核 69 列 schema、旧 CSV 迁移备份、manifest 字段和 EvidenceLogger schema。合并前可用一份旧 25 列空表和一份含旧分数的表各跑一次迁移，确认 legacy 字段没有被映射进新版门槛；同时决定运行数据目录的备份与归档规则。Research 的 `_pipeline_meta.stage_status`、`known_binder_source` 和 `calibration_status` 应作为展示层的固定字段保留。

于嘉乐负责 Design 时，应让三条路线统一调用候选注册逻辑并产出 manifest。RFpeptides/ProteinMPNN 等模型适配器需要返回模型版本、命令参数、输入结构哈希、输出路径、退出码和序列来源；任何 fallback 都要换一个清楚的来源标签。Route A 的 cyclic offset 结果进入 Prediction 前仍需真实首尾闭环结构；Route B 在适配器未完成时允许返回 0 条有效候选；Route C 当前只是一组确定性序列提案，不能作为已完成结构设计的替代品。

王修远负责 Prediction 和组长决策时，可以从 `CandidateIndex.find(candidate_id)` 取得 manifest 和设计 PDB。对每个候选分别生成 MDM2、MDMX 记录并填写 `ipsae_*`、`dg_*`、`sc_*`、`dsasa_*`、`site_consistency_*`、`pose_rmsd_*` 与 `seed_convergence_*`；L4 在 relax 前后写入数值距离；L7 的 scRMSD 必须记录比较的两份结构。完成一条候选后调用 `evaluate_battery(candidate, thresholds)`，正对照则显式传单靶 `required_targets`。组长筛选时先看七层通过和 Pareto front，再结合可合成性、模型间一致性和证据完整度形成最终交付集合。

## 八、验证命令

数据层测试使用系统 Python 即可，当前结果为 161/161：

```bash
python3 test_data_layer.py
```

Research/Design 回归测试需要安装 biotite 的项目环境，当前结果为 7/7：

```bash
./.venv/bin/python -m unittest -v test_reliability_regressions.py
```

完整 Research 应明确调用 `recompute()`，它会绕过旧 `_research_cache.json`：

```bash
python -c "from agents.research import recompute; recompute()"
```

测试和 Research 实跑均不代表 RFpeptides、ProteinMPNN、AF3、Rosetta 或完整 Prediction 已经可用；这些工具需要在项目服务器的固定环境中分别验证。
