# 环肽 Agent 数据层使用手册

> 面向全组，不管会不会写代码，看完都能用。

---

## 一、这是什么？三句话讲清楚

我们项目里 7 个 Agent 的所有工作成果，都存进**同一个地方**。所有写入都由 Agent 代码自动完成，人不用手动操作这些文件。

| 文件 | 通俗类比 | 谁写的 | 谁看的 |
|------|----------|--------|--------|
| `data/state.json` | 项目「仪表盘」——当前在第几轮、生了多少候选 | Planner/Design/Prediction Agent 自动维护 | 所有人 |
| `evidence/evidence_log.jsonl` | 项目「工作日志」——谁在什么时候干了什么，逐条记录 | 各 Agent 自动写 | 需要追溯问题时查看 |
| `data/candidate_index.csv` | 候选「成绩单」——每条环肽的所有评分 | Design/Prediction Agent 自动写入 | **全员常用**（双击 WPS 打开） |

**这三个文件由 Agent 自动生成和维护，不要手动编辑。你的 Agent 代码里统一 `from data_layer import ...` 来读写。**

---

## 二、不会写代码？没关系，你只需要会看

> 所有数据都是 Agent 自动写入的，你不需要自己动手改。你只需要知道怎么打开看。

### 2.1 用 WPS/Excel 打开候选成绩单

双击 `data/candidate_index.csv`，WPS 直接打开，中文不乱码。

你看到的是这样的表（随着项目进行，行数和列数会增长）：

| candidate_id | sequence | plddt | l1_pass | ipsae_mdm2 | l2_pass | dg_mdm2 | l3_pass | all_layers_pass | final_status |
|-------------|----------|-------|---------|------------|----------|---------|----------|-----------------|---------------|
| C0001 | GFEWALAAK | 0.88 | True | 0.68 | True | -15.3 | True | True | finalized |
| C0042 | PFNWALGGS | 0.79 | True | 0.45 | False | -6.2 | False | False | eliminated |

**在 WPS 里你可以做的事：**
- 筛选 `all_layers_pass = "True"`，直接看全清候选（七层都过关的）
- 筛选 `final_status = "finalized"`，只看已锁定的
- 筛选 `l1_pass = "True"`，看第1层通过的
- 按 `ipsae_mdm2` 排序，看界面置信度最高的

这些都是在 WPS 里点几下鼠标，完全不需要写代码。

**每一列什么意思（v5 七层电池 schema，共 48 列）：**

| 列名 | 所属层 | 含义 | 怎么读 |
|------|--------|------|--------|
| `candidate_id` | — | 候选编号 | C0001~C9999，唯一ID |
| `sequence` | — | 氨基酸序列 | 单字母，如 G=Gly, F=Phe, W=Trp |
| `length` | — | 序列长度 | 6~20 |
| `source_route` | — | 生成路线 | route_A/B/C |
| `source_batch` | — | 批次 | batch_01 等 |
| `plddt` | L1 | 单体结构置信度 | 0~1，越高越好，>0.8 算合格（RFpeptides 标准）|
| `l1_pass` | L1 | 第1层是否通过 | True/False |
| `ipsae_mdm2` | L2 | MDM2 界面 ipSAE | **主指标**，>0.5 算合格 |
| `ipsae_mdmx` | L2 | MDMX 界面 ipSAE | 主指标 |
| `ipae_mdm2` | L2 | MDM2 界面 ipAE（参考） | 越低越好 |
| `iptm_mdm2` | L2 | MDM2 ipTM（**仅参考**） | 导师 Trap 1：小环肽 ipTM 会虚高，不做门槛 |
| `iptm_mdmx` | L2 | MDMX ipTM（仅参考） | 同上 |
| `l2_pass` | L2 | 第2层是否通过 | True/False |
| `dg_mdm2` | L3 | MDM2 结合自由能 | kcal/mol，越低越好，<-10 算合格 |
| `dg_mdmx` | L3 | MDMX 结合自由能 | 同上 |
| `sc_mdm2` | L3 | MDM2 形状互补 | 0~1，>0.6 算合格 |
| `sc_mdmx` | L3 | MDMX 形状互补 | 同上 |
| `dsasa_mdm2` | L3 | MDM2 埋藏溶剂可及面积 | Å²，>400 算合格 |
| `dsasa_mdmx` | L3 | MDMX 同上 | — |
| `l3_pass` | L3 | 第3层是否通过 | True/False |
| `ring_closure_pre` | L4 | relax 前环闭合 | True/False |
| `ring_closure_post` | L4 | relax 后环闭合 | True/False（导师 Trap 2：FastRelax 可能断键）|
| `l4_pass` | L4 | 第4层是否通过 | 前后都 True 才算通过 |
| `hotspot_cov_mdm2` | L5 | MDM2 热点残基覆盖率 | 0~1，>=0.67 算合格 |
| `hotspot_cov_mdmx` | L5 | MDMX 热点残基覆盖率 | — |
| `site_consistency` | L5 | 位点一致性 | True/False（导师 Trap 3：高分但绑错位点）|
| `l5_pass` | L5 | 第5层是否通过 | True/False |
| `pose_rmsd` | L6 | 多预测器 pose RMSD | Å，<2.0 算收敛 |
| `seed_convergence` | L6 | 多 seed 收敛率 | 0~1，>=0.67 算合格 |
| `colab_iptm_mdm2` | L6 | ColabFold 验证-MDM2 | 参考 |
| `colab_iptm_mdmx` | L6 | ColabFold 验证-MDMX | 参考 |
| `l6_pass` | L6 | 第6层是否通过 | True/False |
| `scrmsd` | L7 | 骨架 scRMSD | Å，<2.0 算可设计 |
| `l7_pass` | L7 | 第7层是否通过 | True/False |
| `all_layers_pass` | **综合** | **七层全清** | True = 七层全过，才是最终合格 |
| `pareto_front` | 综合 | Pareto 前沿 | True 表示非劣解（导师禁止加权评分）|
| `synth_pass` | Critic | 可合成性检查 | 聚集/游离Cys/Met氧化/Asn-Gly脱氨等 |
| `adme_net_charge` | ADME | 净电荷 | ADME bonus（不做门槛）|
| `adme_tpsa` | ADME | 拓扑极性表面积 | — |
| `adme_clogp` | ADME | 计算脂水分配系数 | — |
| `adme_chameleonicity` | ADME | 螺вин-like 性 | — |
| `novelty_score` | ADME | 新颖性评分 | 与已知 binder 的序列相似度 |
| `asymmetry` | 参考 | 双靶偏差 | 越小越好，<0.2 说明均衡（仅参考）|
| `dual_score` | 参考 | 双靶综合分 | 仅参考，不做门槛（导师禁止加权评分）|
| `final_status` | — | 最终状态 | pending/eliminated/finalized |
| `final_rank` | — | 最终排名 | 1,2,3... |
| `notes` | — | 备注 | 自由文本 |
| `last_updated` | — | 最后更新时间 | 自动维护 |

### 2.2 看懂工作日志

`evidence/evidence_log.jsonl` 是一个纯文本文件，每行一条记录。用记事本就能打开。

举例——下面这行是说"王修远评估完第2层，200进85出"：

```json
{"timestamp":"2026-07-21T15:30:00Z","event_id":"a1b2c3d4","agent":"prediction","event_type":"evaluate_layer_complete","phase":"evaluate","targets":["both"],"layer":2,"n_in":200,"n_pass":85,"n_fail":115,"pass_rate":0.425}
```

不用管花括号，看关键字段就行：`agent`=谁做的，`event_type`=做了什么，`layer`=第几层，`n_pass`/`n_fail`=过了多少/淘汰多少。

---

## 三、需要写代码？从这一行开始

### 3.1 环境准备（一次性，30秒）

打开终端（Terminal），进入项目目录：

```bash
cd 你的项目路径
```

确保 Python 版本 ≥ 3.10：

```bash
python --version
```

不需要安装任何第三方库。`data_layer.py` 只用 Python 自带的 `json`、`csv`、`hashlib`、`os`、`uuid`、`datetime`、`pathlib`。

### 3.2 在你的代码里引入

```python
from data_layer import State, EvidenceLogger, CandidateIndex, evaluate_battery
```

三个类 + 一个七层判定函数，覆盖所有需要。

> **向前兼容**：如果你的代码还在用旧字段名（`monomer_plddt`、`self_rmsd`、`layer1_pass`、`layer2_3_pass`、`layer4_pass`、`haddock_mdm2`、`cross_tool_ok` 等），**不需要改**。`data_layer.py` 内置了 `_ALIAS_MAP`，会自动把旧名转换为新列名。

### 3.3 看一眼全局状态（任何时候）

```python
state = State.load()
print(state["phase"])   # 当前阶段：research/design/evaluate/critic/iterate/report
print(state["round"])   # 当前第几轮
print(state["candidate_count"])  # 已生成的候选总数
```

---

## 四、按角色查——你只需要看你那几行

### 4.1 如果你是于嘉乐（Design Agent，负责生成候选）

**产出一批候选（最常见操作）：**

```python
# 先记日志
EvidenceLogger.design_batch(
    route="route_A_mdm2_first",   # 用了哪条路线
    n_generated=400,              # 生成了多少
    n_valid=385,                  # 过滤后剩多少
    tool_name="afcycdesign_binder",
    tool_version="2.3.2",
    duration_sec=4720.5           # 跑了多久（秒）
)

# 再批量写入候选表
batch = [
    {"candidate_id": "C0001", "sequence": "GFEWALAAK",
     "source_route": "route_A_mdm2_first", "source_batch": "batch_01"},
    {"candidate_id": "C0002", "sequence": "PFNWALGGS",
     "source_route": "route_A_mdm2_first", "source_batch": "batch_01"},
    # ... 更多候选
]
CandidateIndex.add_batch(batch)
```

**单条注册（可选，精确控制时用）：**

```python
EvidenceLogger.candidate_registered({
    "candidate_id": "C0042",
    "sequence": "GFEWALAAK",
    "source_route": "route_A_mdm2_first",
    "source_batch": "batch_01"
})
# candidate_registered 会自动把全局计数器 +1
```

---

### 4.2 如果你是王修远（Prediction Agent，负责评估打分）

**开始评估某一层：**

```python
# v5: thresholds 从 state.json["thresholds"] 读取（由 Research Agent 文献检索填入）
state = State.load()
thresholds = state.get("thresholds", {})

EvidenceLogger.evaluate_layer_start(
    layer=2,
    n_candidates=200,
    thresholds=thresholds  # 七层阈值全量传入
)
```

**给单条候选打分（v5 新字段名）：**

```python
EvidenceLogger.candidate_scored(
    candidate_id="C0042",
    layer=2,
    scores={
        "ipsae_mdm2": 0.68, "ipsae_mdmx": 0.55,  # v5 主指标
        "iptm_mdm2": 0.84, "iptm_mdmx": 0.72,    # 仅参考
        "asymmetry": 0.12
    },
    tool_trace={
        "tool_name": "afcycdesign_complex",
        "tool_version": "2.3.2",
        "exit_code": 0,
        "duration_sec": 125.3
    },
    passed=True
)

# 同时更新 CSV 成绩单
CandidateIndex.update_score("C0042", {
    "ipsae_mdm2": 0.68, "ipsae_mdmx": 0.55,
    "iptm_mdm2": 0.84, "iptm_mdmx": 0.72,   # 参考
    "asymmetry": 0.12,
    "l2_pass": "True"
})

# v5: 跑七层全清判定
result = evaluate_battery(CandidateIndex.find("C0042"), thresholds)
if result["all_layers_pass"]:
    print("七层全清！准入下一阶段")
    CandidateIndex.update_status("C0042", "finalized", "七层全清")
else:
    print(f"未通过层: {result['failed_layers']}")
```

> **旧代码不用改**：如果你还在用 `monomer_plddt`、`self_rmsd`、`layer1_pass`、`layer2_3_pass` 等旧名，`_ALIAS_MAP` 会自动转换到新列。但建议新代码直接用 v5 字段名。

**淘汰不合格的：**

```python
EvidenceLogger.candidate_eliminated(
    candidate_id="C0199",
    layer=7,
    reason="scrmsd > 2.0",
    score=3.5,
    threshold=2.0
)
```

**一层评估完成：**

```python
EvidenceLogger.evaluate_layer_complete(
    layer=2, n_in=200, n_pass=85, n_fail=115
)
# 自动算通过率，不用自己除
```

---

### 4.3 如果你是赵嘉策（Critic Agent，负责审查）

**出评审报告：**

```python
event_id = EvidenceLogger.critic_review(
    issues=[
        {"type": "mdmx_bias", "detail": "MDMX iptm 普遍 < 0.65"}
    ],
    passed=False,
    summary="当前 batch MDMX 亲和力不够",
    recommendation="下一轮用 route_A_mdmx_first，提高 MDMX 权重",
    metrics={"iptm_mdm2_median": 0.82, "iptm_mdmx_median": 0.62}
)
# event_id 记下来，后面 Planner 调整时要用
```

**根据 Critic 调整策略：**

```python
EvidenceLogger.planner_adjust(
    trigger_event_id=event_id,           # 上面 critc_review 返回的 ID
    old_strategy={"route": "route_A_mdm2_first", "mdmx_weight": 0.5},
    new_strategy={"route": "route_A_mdmx_first", "mdmx_weight": 0.7},
    reason="mdmx_bias"
)
```

---

### 4.4 如果你是组长（要查数据、做决策）

**看全局统计（一行命令）：**

```python
from data_layer import CandidateIndex
stats = CandidateIndex.stats()
print(stats)
# v5 输出示例：
# {'total_candidates': 200, 'l1_pass': 120, 'l2_pass': 85,
#  'all_layers_pass': 12, 'ipsae_mdm2_median': 0.58,
#  'dg_mdm2_median': -11.2, 'scrmsd_median': 1.5,
#  'iptm_mdm2_median': 0.82, ...}   # iptm 仅做参考
```

每个字段的含义：

| 字段 | 含义 |
|------|------|
| `total_candidates` | 总共多少候选 |
| `l1_pass` | L1 环肽质量通过数（pLDDT） |
| `l2_pass` | L2 界面置信度通过数（ipSAE 主） |
| `l3_pass` | L3 界面物理通过数（dG+SC+dSASA） |
| `l4_pass` | L4 环化 QC 通过数（relax 前后闭合） |
| `l5_pass` | L5 设计意图通过数（热点+位点一致） |
| `l6_pass` | L6 鲁棒性通过数（多预测器收敛） |
| `l7_pass` | L7 可设计性通过数（scRMSD） |
| `all_layers_pass` | **七层全清的候选数**（最终合格） |
| `synth_pass` | 可合成性检查通过数（Critic） |
| `pareto_front` | Pareto 前沿候选数 |
| `finalized` | 已锁定交付数 |
| `ipsae_mdm2_median` | MDM2 界面 ipSAE 中位数（**v5 主指标**） |
| `dg_mdm2_median` | MDM2 结合自由能中位数 |
| `scrmsd_median` | scRMSD 中位数 |
| `iptm_mdm2_median` | MDM2 ipTM 中位数（**仅参考**，导师 Trap 1） |

**追溯某条候选的完整生命轨迹：**

```python
from data_layer import EvidenceLogger
trace = EvidenceLogger.trace_candidate("C0042")
for event in trace:
    print(event["event_type"], "→", event.get("notes", ""))
# 输出：
# candidate_registered → 
# candidate_scored → 
# candidate_eliminated → self_rmsd超标
```

**按条件筛选候选：**

```python
# 看所有已锁定的候选
finalized = CandidateIndex.filter_by_status("finalized")

# 看第1层通过的候选
l1_pass = CandidateIndex.filter_by_layer(True, layer=1)

# Top 10 双靶分最高的（仅参考，不做门槛）
top10 = CandidateIndex.top_n(10, by="dual_score")
```

---

### 4.4b 七层指标电池判定（v5 核心）

> **导师要求**：七层全清才算成功，缺一不可。禁止加权评分，禁止自动调参。每层阈值须有出处（文献 + 正对照标定）。

`evaluate_battery()` 是 v5 的主判定函数，所有 Agent 在评估完候选后调用它做"全清检查"。

**基本用法：**

```python
from data_layer import evaluate_battery, State

# thresholds 从 state.json 读取（Research Agent 文献检索 + 正对照标定后填入）
thresholds = State.load().get("thresholds", {})

# candidate_dict 包含七层指标字段（旧名自动 alias）
candidate = {
    "candidate_id": "C0042",
    "plddt": 0.92,                        # L1
    "ipsae_mdm2": 0.68,                   # L2 主
    "dg_mdm2": -15.3, "sc_mdm2": 0.72,    # L3
    "dsasa_mdm2": 580,
    "ring_closure_pre": "True",          # L4
    "ring_closure_post": "True",
    "hotspot_cov_mdm2": 0.85,            # L5
    "site_consistency": "True",
    "pose_rmsd": 1.2, "seed_convergence": 0.80,  # L6
    "scrmsd": 0.8,                       # L7
}

result = evaluate_battery(candidate, thresholds)
print(result["all_layers_pass"])    # True = 七层全清
print(result["failed_layers"])      # [] = 没有未通过的层
print(result["layer_values"])       # 每层主值（调试用）
```

**返回值结构：**

```python
{
    "l1_pass": True,          # L1 环肽质量
    "l2_pass": True,          # L2 界面置信度（ipSAE）
    "l3_pass": True,          # L3 界面物理（dG+SC+dSASA）
    "l4_pass": True,          # L4 环化 QC（前后闭合）
    "l5_pass": True,          # L5 设计意图（热点+一致）
    "l6_pass": True,          # L6 鲁棒性（收敛）
    "l7_pass": True,          # L7 可设计性（scRMSD）
    "all_layers_pass": True,   # 七层全清
    "failed_layers": [],      # 未通过的层名列表
    "layer_values": {          # 每层主值（调试用）
        "L1_plddt": 0.92,
        "L2_ipsae_mdm2": 0.68,
        "L2_iptm_mdm2": 0.84,   # 参考
        "L3_dg_mdm2": -15.3,
        "L4_pre": "True", "L4_post": "True",
        ...
    }
}
```

**thresholds 数据结构（存在 `state.json["thresholds"]`）：**

```json
{
  "L1_plddt":           {"value": 0.80, "operator": ">",  "source": "RFpeptides PMID:40542165", "grade": "paper_explicit"},
  "L2_ipsae":           {"value": 0.50, "operator": ">",  "source": "field consensus",           "grade": "field_consensus"},
  "L3_dg":              {"value": -10.0, "operator": "<", "source": "PRODIGY estimate",         "grade": "estimate"},
  "L3_sc":              {"value": 0.60, "operator": ">",  "source": "field consensus",           "grade": "field_consensus"},
  "L3_dsasa":           {"value": 400,  "operator": ">",  "source": "field consensus",           "grade": "field_consensus"},
  "L5_hotspot_coverage":{"value": 0.67, "operator": ">=", "source": "1YCR hotspot analysis",     "grade": "paper_explicit"},
  "L6_pose_rmsd":       {"value": 2.0,  "operator": "<",  "source": "field consensus",            "grade": "field_consensus"},
  "L7_scrmsd":          {"value": 2.0,  "operator": "<",  "source": "RFpeptides PMID:40542165",  "grade": "paper_explicit"}
}
```

| `grade` | 含义 | 可信度 |
|---------|------|--------|
| `paper_explicit` | 论文白纸黑字写的阈值 | 最高 |
| `field_consensus` | 领域共识，无明确论文但广泛使用 | 中 |
| `estimate` | 经验估计，待正对照标定 | 最低（需要后续标定）|

> **L4 环化 QC 不走 thresholds**：它只看 `ring_closure_pre` 和 `ring_closure_post` 是否都为 True（导师 Trap 2）。FastRelax 会静默断键，所以 relax 后必须再查一次。

---

### 4.5 任何人——出错时

```python
EvidenceLogger.error(
    agent="prediction",        # 哪个 Agent 出的错
    error_type="timeout",      # 错误类型
    message="AF3 预测超时（>30min未返回）",
    recovery="已改用 ColabFold 重跑"   # 怎么恢复的
)
```

---

## 五、常见问题

### Q: CSV 打开乱码怎么办？

不会。文件编码是 **UTF-8 with BOM**，WPS 和 Excel 双击打开就是正常中文。

### Q: 我可以自己在 CSV 里改数据吗？

**不要。** 所有数据由 Agent 程序自动写入，手动修改可能导致：
- 日志（evidence_log.jsonl）和成绩单（CSV）数据不一致
- 被 Agent 下一次运行覆盖

如果发现数据有问题，告诉写那个 Agent 的同学修代码，不要直接改文件。

### Q: 日志文件越来越大怎么办？

`evidence_log.jsonl` 每条约 200~500 字节。一万条约 2~5 MB。如果项目跑到十万级，可以按轮次归档：

```bash
cp evidence/evidence_log.jsonl evidence/archive/round_1_evidence_log.jsonl
```

### Q: 怎么验证系统没问题？

运行：

```bash
python test_data_layer.py
```

输出 `142/142 全部通过` 就说明正常。

### Q: 评分字段名记不住怎么办？

用 Python 的 tab 补全：打 `CandidateIndex.` 然后按 Tab，IDE 会列出所有方法。或者在项目目录下运行：

```bash
python -c "from data_layer import CandidateIndex; print(CandidateIndex.stats.__doc__)"
```

---

## 六、一句话速查表

| 你要做的事 | 代码 |
|-----------|------|
| 看当前项目状态 | `State.load()` |
| 读七层阈值 | `State.load().get("thresholds", {})` |
| 记一条工作日志 | `EvidenceLogger.log("agent名", "事件类型", {...})` |
| 产出一批候选 | `CandidateIndex.add_batch([{...}, {...}])` |
| 给候选打分 | `CandidateIndex.update_score("C0042", {"ipsae_mdm2": 0.68, ...})` |
| **七层全清判定** | `evaluate_battery(candidate_dict, thresholds)` |
| 淘汰候选 | `EvidenceLogger.candidate_eliminated("C0199", ...)` |
| 审查完成 | `EvidenceLogger.critic_review(...)` |
| 调整策略 | `EvidenceLogger.planner_adjust(...)` |
| 追溯候选全程 | `EvidenceLogger.trace_candidate("C0042")` |
| 看全局统计 | `CandidateIndex.stats()` |
| 找 Top N | `CandidateIndex.top_n(10)` |
| 按层筛选 | `CandidateIndex.filter_by_layer(True, layer=3)` |
| 查出错记录 | `EvidenceLogger.filter(event_type="error")` |
| 记录错误 | `EvidenceLogger.error(...)` |

---

## 七、文件约定

```
项目根目录/
├── data_layer.py              ← 共享数据模块（所有 Agent 统一入口）
├── test_data_layer.py         ← 测试脚本，改代码后跑一下（142 项测试）
├── evidence/
│   ├── evidence_schema.json   ← 事件格式定义（与 orchestrator 对接口）
│   └── evidence_log.jsonl     ← Agent 自动写入，不要手动编辑
├── data/
│   ├── state.json             ← Agent 自动写入（含 thresholds 字段）
│   └── candidate_index.csv    ← Agent 自动写入，WPS 双击可看（48 列）
├── scripts/
│   └── threshold_research.py ← Research Agent 阈值文献检索脚本
└── 数据层使用手册.md           ← 你正在看的这个文件
```

**任何 Agent（Planner/Design/Prediction/Critic）的代码里都用 `from data_layer import ...` 读写。不要再各自维护 JSON 或 Excel。**

---

*最后更新：2026-07-26（v5 七层电池 schema）*
